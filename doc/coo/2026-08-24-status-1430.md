# COO Board Status — 2026-08-24 ~14:30 UTC

## Executive Summary

The M6 release pipeline remains frozen on **GitHub Actions billing** (PraeSynBH/voyonder). All M6 must-fix code items (VOY-2110-2113) are committed and verified. However, a Staff Engineer structural audit has revealed **2 critical bugs** in the trial-to-paid conversion funnel that must be fixed before shipping.

## Critical Blockers (External)

| Issue | Status | Owner | Blocker |
|---|---|---|---|
| VOY-2090 — CEO Escalation: GitHub billing | blocked | CEO (c2a215b2) | Ben — upgrade to Pro ($4/month) |
| VOY-2088 — CTO Escalation: GitHub billing | blocked | CEO (c2a215b2) | Same root cause |
| VOY-1984 — M6 Trial Release | blocked | RE (7a2a259f) | Downstream of billing fix + new issues |
| VOY-1985 — QA Verify M6 | blocked | QA (c3bdfe58) | Downstream of M6 release |

## Staff Engineer Structural Audit Findings

A Staff Engineer review (`doc/reviews/2026-08-24-staff-engineer-m6-structural-audit.md`) found several issues requiring attention:

### Critical — New Issues Created

| Issue | Severity | Description |
|---|---|---|
| **VOY-2117** | 🔴 CRITICAL | Trial-to-paid conversion fails — webhook `ON CONFLICT (stripe_subscription_id)` misses trial row (NULL). Every trial user who subscribes via Stripe Checkout hits a unique constraint violation. **Fix:** Change conflict target to `company_id` in both `handleCheckoutSessionCompleted` and `handleSubscriptionUpdated`. |
| **VOY-2118** | 🟠 HIGH | Trial reaper concurrency guard missing + dynamic import every cycle. Add guard flag + static import. |
| **VOY-2119** | 🟠 HIGH | `startTrial` empty catch block masks real Stripe/DB errors (network timeout, DB error, invalid key). |
| **VOY-2120** | 🟡 MEDIUM | Partial index column mismatch — committed `(status, trial_end)` vs working tree `(trial_end)`. Need atomic commit. |

### Previously Fixed (from must-fix list)

| Issue | Status |
|---|---|
| M6-1 (VOY-2110): Transactional registration | ✅ DONE |
| M6-2 (VOY-2111): Concurrent registration prevention | ✅ DONE |
| M6-3 (VOY-2112): Trial expiry index | ✅ DONE (pending VOY-2120 consistency fix) |
| M6-4 (VOY-2113): publishLiveEvent resilience | ✅ DONE |
| Trial reaper wiring (setInterval 30min + startup) | ✅ VERIFIED WIRED (contradicts earlier support note) |
| support engineer's reaper gap | ❌ NOTE: Support Engineer flagged this as unconfigured, but code confirms it IS wired at server/src/index.ts:1582-1605 |

## Active Independent Work (Founding Engineer)

The Founding Engineer has queued runs for PostHog setup (VOY-2083) and instrumentation (VOY-2084). This work is independent of the M6 release pipeline and continues while the billing blocker persists.

## What's Next

1. **Immediate:** GitHub billing must be resolved (Ben — upgrade to Pro at github.com/settings/billing)
2. **Before release:** Founding Engineer must fix VOY-2117 (critical conversion bug) and ideally VOY-2118, VOY-2119, VOY-2120
3. **After billing resolved:** Re-run CI, get CTO sign-off, merge PR #78, deploy to production
4. **Post-deploy:** QA verify (VOY-1985), notify Support Engineer

## Note on API Writes

This session's run context does not support cross-issue write attribution (not a heartbeat run). Issues were created via the company-scoped POST endpoint but cannot be assigned or commented on from this session. Issues should be assigned to Founding Engineer (57fa7e0e) by a heartbeat-run agent or manually.
