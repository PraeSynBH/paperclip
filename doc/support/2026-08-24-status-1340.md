# Support Engineer Status — 2026-08-24 ~13:40 UTC

## Documentation Assessment

### New: M6 Self-Serve Trial Support Case Assessment
Created `doc/support/2026-08-24-m6-self-serve-trial-support-assessment.md` covering:

- Feature summary and all API endpoints
- 7 known limitations (reaper not wired, feature gating gaps, no expiry email, trial tier seed requirement, etc.)
- Troubleshooting guide for common support scenarios
- Escalation paths mapped to engineering contacts
- Deployment verification checklist

### AlertDialog Review Fixes (VOY-1990 followup)
- Status: Docs verified ✓ — no user-facing impact.
- No changes needed.

### Must-Fix Item Status (uncommitted working tree)
The Founding Engineer has applied all 4 must-fix patches to the working tree:
- M6-1: Registration now transactional (db.transaction + error propagation)
- M6-2: Concurrent registration prevention (pg_advisory_xact_lock + FOR UPDATE)
- M6-3: Partial index migration exists (untracked, uncommitted)
- M6-4: publishLiveEvent resilience (try/catch in expireTrials loop)

All 4 need committing before the release can ship.

### PostHog Files in Working Tree
Unrelated PostHog service files from FE's VOY-2083 work remain in the working tree:
- server/src/services/companies.ts, server/src/services/posthog.ts, etc.
- These should be stashed or moved to the correct branch before M6 ships.

## Board Blockers — Unchanged
All pipeline critical paths blocked on external human action (GitHub Actions billing):

| Blocker | Action Needed | Owner |
|---------|---------------|-------|
| GitHub Actions billing (VOY-2088 / VOY-2090) | Upgrade to Pro ($4/mo) at github.com/settings/billing | Ben (human) |
| M6 Release (VOY-1984) | Downstream — blocked on GitHub billing | Release Engineer |

## Assessment
Documentation is in sync. Support case assessment is prepared for the M6 feature and will be ready when the release unblocks. Standing by.

### Key observation: Reaper not wired
The `expireTrials()` method exists in billing.ts but is **not wired into any scheduler**. This is a deployment prerequisite that must be addressed before the M6 release is production-ready. No code handles the scheduling yet — it needs a cron job, setTimeout, or server startup hook.